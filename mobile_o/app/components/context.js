import React from "react";

export const AuthContext = React.createContext(undefined);
export const TokenContext = React.createContext(undefined);
export const LoginValidationContext = React.createContext(undefined);
